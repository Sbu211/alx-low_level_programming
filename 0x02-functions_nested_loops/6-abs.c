#include "main.h"

/**
 * _abs - check main
 * @i: input in inter formm
 * Description: get the absolute value
 * Return: |int| is returned
 */


int _abs(int i)
{
	if (i >= 0)

		return (i);

	else

		return (i * -1);
}
